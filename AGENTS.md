# Repository instructions

- Run `npm run format` after editing project files.
- Before handing work off, run `npm run format:check`, `npm run lint`, `npm run typecheck`, and `npm run build`.
- Write commit messages using the Conventional Commits specification (for example, `feat:`, `fix:`, `docs:`, `style:`, `refactor:`, `test:`, or `chore:`).
